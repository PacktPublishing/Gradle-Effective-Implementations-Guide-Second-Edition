apply plugin: 'scala'

repositories {
    jcenter()
}

dependencies {
    compile "org.scala-lang:scala-library:2.11.4"
}

sourceSets {
    actors
}

// Add classes compiled from the
// actors source set to the Jar.
jar.dependsOn 'actorsClasses'
