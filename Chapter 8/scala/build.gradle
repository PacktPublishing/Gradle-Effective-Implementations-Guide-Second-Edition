apply plugin: 'scala'

repositories {
    jcenter()
}

dependencies {
    // Define dependency on Scala library
    // for compilation and Scala tools.
    compile group: 'org.scala-lang',
            name: 'scala-library',
            version: '2.11.4'
}
